{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "_comment": "Matches docker-compose.yml's local-only Postgres container — not a secret (NFR-402): a throwaway local container protects nothing. Must move to Key Vault + managed identity (docs/03-architecture/06-security-strategy.md §5) before any deployment beyond localhost.",
    "Postgres": "Host=localhost;Port=5432;Username=atlas;Password=atlas_local_dev;Database=atlas"
  }
}
